node_modules
.next
.vercel
out
dist
.env
/generated/prisma
.env.local
.env.*.local
# Real deploy compose — contains live secrets as literals. Template is docker-compose.example.yml.
docker-compose.yml
# Fully self-contained image with secrets baked into layers. Template is Dockerfile.example.
Dockerfile.private
*.tsbuildinfo
next-env.d.ts
prisma/migrations/**/migration_lock.toml
!prisma/migrations/**/migration.sql

# Visual brainstorm companion (superpowers)
.superpowers/

# Nested git worktrees created by the agent harness
.claude/worktrees/
